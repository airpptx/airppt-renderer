/**
 * Powerpoint converts images into Tiffs, we need to convert them back to PNGs when writing our content to support HTML
 */
export default function convertToPNGandWrite(inputPath: any, outputPath: any): void;
